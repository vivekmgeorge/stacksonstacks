<h1>New Comment</h1>

<%= form_for @comment do |f| %>
	<p>
		<%= f.label :body %>
		<%= f.text_area :body, cols: 15, rows: 7 %>
	</p>
	
	<p>
		<%= f.submit %>
	</p>
<% end %>

<%= link_to '<< back', :back %>